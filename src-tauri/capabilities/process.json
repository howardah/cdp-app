{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "process-capability",
  "description": "Process windows receive only application IPC and core window APIs",
  "windows": ["process-*"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
