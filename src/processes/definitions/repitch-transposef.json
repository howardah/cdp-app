{
  "schemaVersion": 1,
  "id": "repitch-transposef",
  "title": "Repitch transposef",
  "category": "spectral",
  "summary": "CDP Repitch transposef operation.",
  "description": "Run the documented CDP Release 8 Repitch transposef operation through the desktop process runner.",
  "useCases": ["Explore a CDP process", "Build an audio-processing workflow"],
  "tags": ["repitch", "transposef", "cdp-release-8"],
  "identity": {
    "executable": "repitch",
    "operation": "transposef"
  },
  "documentation": {
    "localPath": "cdpr8/docs/html/crepitch.htm",
    "helpCommand": ["repitch", "transposef"],
    "verifiedWithVersion": "CDP Release 8"
  },
  "modes": [
    {
      "id": "process",
      "title": "Repitch transposef",
      "summary": "Run Repitch transposef.",
      "inputs": [
        {
          "id": "source",
          "label": "Analysis file",
          "description": "Analysis file whose original spectral envelope will be retained.",
          "fileTypes": ["analysis-ana"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "formantPoints",
          "label": "Formant points",
          "description": "One formant-envelope point per N equally spaced frequency channels.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "option", "flag": "-f", "join": "concatenated" },
          "kind": "integer",
          "default": 4,
          "min": 1,
          "step": 1
        },
        {
          "id": "semitones",
          "label": "Transposition",
          "description": "Transposition in semitones.",
          "required": true,
          "advanced": false,
          "unit": "semitones",
          "cli": { "kind": "positional" },
          "kind": "number",
          "default": 0,
          "step": 0.01
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "analysis-ana",
        "extension": "ana",
        "nameSuffix": "-transposef"
      },
      "argumentOrder": [
        {
          "kind": "literal",
          "value": "transposef"
        },
        { "kind": "literal", "value": "3" },
        {
          "kind": "input",
          "inputId": "source"
        },
        {
          "kind": "output"
        },
        { "kind": "parameter", "parameterId": "formantPoints" },
        { "kind": "parameter", "parameterId": "semitones" }
      ],
      "constraints": []
    }
  ]
}
