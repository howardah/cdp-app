{
  "schemaVersion": 1,
  "id": "blur-noise",
  "title": "Add Spectral Noise",
  "category": "spectral",
  "summary": "Add noise-like energy to a spectrum.",
  "description": "Randomize a PVOC spectrum toward a noise character at a controlled amount.",
  "useCases": ["Add noisy texture", "Deharmonize a tone"],
  "tags": ["noise", "spectrum", "random"],
  "identity": { "executable": "blur", "operation": "noise" },
  "documentation": {
    "localPath": "cdpr8/docs/html/cblur.htm",
    "anchor": "NOISE",
    "helpCommand": ["blur", "noise"],
    "verifiedWithVersion": "CDP Release 7.1"
  },
  "modes": [
    {
      "id": "noise",
      "title": "Noise",
      "summary": "Add noise to the spectrum.",
      "inputs": [
        {
          "id": "source",
          "label": "Analysis file",
          "description": "PVOC analysis file.",
          "fileTypes": ["analysis-ana"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "amount",
          "label": "Noise amount",
          "description": "Zero leaves the spectrum unchanged; one saturates it with noise.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "number",
          "default": 0.25,
          "min": 0,
          "max": 1,
          "step": 0.01
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "analysis-ana",
        "extension": "ana",
        "nameSuffix": "-noise"
      },
      "argumentOrder": [
        { "kind": "literal", "value": "noise" },
        { "kind": "input", "inputId": "source" },
        { "kind": "output" },
        { "kind": "parameter", "parameterId": "amount" }
      ],
      "constraints": []
    }
  ]
}
