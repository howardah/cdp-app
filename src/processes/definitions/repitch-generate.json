{
  "schemaVersion": 1,
  "id": "repitch-generate",
  "title": "Repitch generate",
  "category": "spectral",
  "summary": "CDP Repitch generate operation.",
  "description": "Run the documented CDP Release 8 Repitch generate operation through the desktop process runner.",
  "useCases": ["Explore a CDP process", "Build an audio-processing workflow"],
  "tags": ["repitch", "generate", "cdp-release-8"],
  "identity": {
    "executable": "repitch",
    "operation": "generate"
  },
  "documentation": {
    "localPath": "cdpr8/docs/html/crepitch.htm",
    "helpCommand": ["repitch", "generate"],
    "verifiedWithVersion": "CDP Release 8"
  },
  "modes": [
    {
      "id": "process",
      "title": "Repitch generate",
      "summary": "Run Repitch generate.",
      "inputs": [
        {
          "id": "source",
          "label": "MIDI pitch data",
          "description": "Text file of increasing time and MIDI-pitch pairs.",
          "fileTypes": ["text-data"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "sampleRate",
          "label": "Sample rate",
          "description": "Sample rate for analysis timing in the generated pitch file.",
          "required": true,
          "advanced": false,
          "unit": "Hz",
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 44100,
          "min": 1,
          "step": 1
        },
        {
          "id": "points",
          "label": "Analysis points",
          "description": "Analysis point count; a power of two from 2 to 32768.",
          "required": false,
          "advanced": true,
          "cli": { "kind": "option", "flag": "-c", "join": "concatenated" },
          "kind": "integer",
          "default": 1024,
          "min": 2,
          "max": 32768,
          "step": 1
        },
        {
          "id": "overlap",
          "label": "Overlap",
          "description": "Analysis overlap factor.",
          "required": false,
          "advanced": true,
          "cli": { "kind": "option", "flag": "-o", "join": "concatenated" },
          "kind": "integer",
          "default": 3,
          "min": 1,
          "max": 4,
          "step": 1
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "binary-pitch",
        "extension": "frq",
        "nameSuffix": "-generate"
      },
      "argumentOrder": [
        {
          "kind": "literal",
          "value": "generate"
        },
        {
          "kind": "input",
          "inputId": "source"
        },
        {
          "kind": "output"
        },
        { "kind": "parameter", "parameterId": "sampleRate" },
        { "kind": "parameter", "parameterId": "points" },
        { "kind": "parameter", "parameterId": "overlap" }
      ],
      "constraints": [
        {
          "kind": "powerOfTwo",
          "value": { "source": "parameter", "id": "points" },
          "message": "Analysis points must be a power of two between 2 and 32768."
        }
      ]
    }
  ]
}
