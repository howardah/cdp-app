{
  "schemaVersion": 1,
  "id": "grain-duplicate",
  "title": "Grain duplicate",
  "category": "time-domain",
  "summary": "CDP Grain duplicate operation.",
  "description": "Run the documented CDP Release 8 Grain duplicate operation through the desktop process runner.",
  "useCases": ["Explore a CDP process", "Build an audio-processing workflow"],
  "tags": ["grain", "duplicate", "cdp-release-8"],
  "identity": {
    "executable": "grain",
    "operation": "duplicate"
  },
  "documentation": {
    "localPath": "cdpr8/docs/html/cgrogrns.htm",
    "helpCommand": ["grain", "duplicate"],
    "verifiedWithVersion": "CDP Release 8"
  },
  "modes": [
    {
      "id": "process",
      "title": "Grain duplicate",
      "summary": "Run Grain duplicate.",
      "inputs": [
        {
          "id": "source",
          "label": "Soundfile",
          "description": "Source required by CDP Grain duplicate.",
          "fileTypes": ["soundfile"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "copies",
          "label": "Copies",
          "description": "Number of copies made for each detected grain.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 2,
          "min": 1,
          "step": 1
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "soundfile",
        "extension": "wav",
        "nameSuffix": "-duplicate"
      },
      "argumentOrder": [
        {
          "kind": "literal",
          "value": "duplicate"
        },
        {
          "kind": "input",
          "inputId": "source"
        },
        {
          "kind": "output"
        },
        { "kind": "parameter", "parameterId": "copies" }
      ],
      "constraints": []
    }
  ]
}
