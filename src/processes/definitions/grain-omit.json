{
  "schemaVersion": 1,
  "id": "grain-omit",
  "title": "Grain omit",
  "category": "time-domain",
  "summary": "CDP Grain omit operation.",
  "description": "Run the documented CDP Release 8 Grain omit operation through the desktop process runner.",
  "useCases": ["Explore a CDP process", "Build an audio-processing workflow"],
  "tags": ["grain", "omit", "cdp-release-8"],
  "identity": {
    "executable": "grain",
    "operation": "omit"
  },
  "documentation": {
    "localPath": "cdpr8/docs/html/cgrogrns.htm",
    "helpCommand": ["grain", "omit"],
    "verifiedWithVersion": "CDP Release 8"
  },
  "modes": [
    {
      "id": "process",
      "title": "Grain omit",
      "summary": "Run Grain omit.",
      "inputs": [
        {
          "id": "source",
          "label": "Soundfile",
          "description": "Source required by CDP Grain omit.",
          "fileTypes": ["soundfile"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "keep",
          "label": "Grains to keep",
          "description": "Number of grains retained in each pattern.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 1,
          "min": 1,
          "step": 1
        },
        {
          "id": "pattern",
          "label": "Pattern length",
          "description": "Total grains in the keep/omit pattern.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 2,
          "min": 2,
          "step": 1
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "soundfile",
        "extension": "wav",
        "nameSuffix": "-omit"
      },
      "argumentOrder": [
        {
          "kind": "literal",
          "value": "omit"
        },
        {
          "kind": "input",
          "inputId": "source"
        },
        {
          "kind": "output"
        },
        { "kind": "parameter", "parameterId": "keep" },
        { "kind": "parameter", "parameterId": "pattern" }
      ],
      "constraints": []
    }
  ]
}
