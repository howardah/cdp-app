{
  "schemaVersion": 1,
  "id": "distort-omit",
  "title": "Distort omit",
  "category": "time-domain",
  "summary": "CDP Distort omit operation.",
  "description": "Run the documented CDP Release 8 Distort omit operation through the desktop process runner.",
  "useCases": ["Explore a CDP process", "Build an audio-processing workflow"],
  "tags": ["distort", "omit", "cdp-release-8"],
  "identity": {
    "executable": "distort",
    "operation": "omit"
  },
  "documentation": {
    "localPath": "cdpr8/docs/html/cdistort.htm",
    "helpCommand": ["distort", "omit"],
    "verifiedWithVersion": "CDP Release 8"
  },
  "modes": [
    {
      "id": "process",
      "title": "Distort omit",
      "summary": "Run Distort omit.",
      "inputs": [
        {
          "id": "source",
          "label": "Soundfile",
          "description": "Source required by CDP Distort omit.",
          "fileTypes": ["soundfile"],
          "minItems": 1,
          "maxItems": 1,
          "ordered": false,
          "constraints": []
        }
      ],
      "parameters": [
        {
          "id": "keep",
          "label": "Cycles to keep",
          "description": "Number of wavecycles retained in each pattern.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 1,
          "min": 1,
          "step": 1
        },
        {
          "id": "pattern",
          "label": "Pattern length",
          "description": "Total wavecycles in the keep/omit pattern.",
          "required": true,
          "advanced": false,
          "cli": { "kind": "positional" },
          "kind": "integer",
          "default": 2,
          "min": 2,
          "step": 1
        }
      ],
      "output": {
        "kind": "singleFile",
        "fileType": "soundfile",
        "extension": "wav",
        "nameSuffix": "-omit"
      },
      "argumentOrder": [
        {
          "kind": "literal",
          "value": "omit"
        },
        {
          "kind": "input",
          "inputId": "source"
        },
        {
          "kind": "output"
        },
        { "kind": "parameter", "parameterId": "keep" },
        { "kind": "parameter", "parameterId": "pattern" }
      ],
      "constraints": []
    }
  ]
}
